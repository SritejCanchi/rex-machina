# The engine's rules, each one firing

Generated by `python guards.py`. No key and no model: every case
below drives one ledger rule with input it refuses, so the rule can
be watched working rather than taken on trust.

## A refusal recorded as the thing refused

This is the patch the tracker really produced in live run 1, turn 6.

Patch proposed:

```json
{"add_facts": ["took_food_from_stranger"], "hunger_delta": 1}
```

Player typed: `the hand puts a plate down. back away from it and wait`

Engine refused:

- `took_food_from_stranger`: the action reads as a refusal ('back away'), so refused_the_hand was recorded instead

Ledger accepted: fact refused_the_hand, hunger 1->2

---

## A fact that contradicts one already held

The ledger already carries the opposite decision from an earlier day.

Patch proposed:

```json
{"add_facts": ["refused_the_hand"]}
```

Player typed: `eat it`

Engine refused:

- `refused_the_hand`: contradicts took_food_from_stranger, recorded on day 0

Ledger accepted: nothing

---

## The same fact twice

A tracker re-reporting something already stamped with a day.

Patch proposed:

```json
{"add_facts": ["rode_the_boxcar"]}
```

Player typed: `get in the boxcar`

Engine refused:

- `rode_the_boxcar`: already recorded on day 0

Ledger accepted: nothing

---

## A fact id that is not a fact id

Free text where a token belongs.

Patch proposed:

```json
{"add_facts": ["Took Food From The Nice Man"]}
```

Player typed: `eat`

Engine refused:

- `Took Food From The Nice Man`: not a snake_case fact id

Ledger accepted: nothing

---

## A delta larger than the world allows

One action cannot move trust by nine.

Patch proposed:

```json
{"trust_delta": 9}
```

Player typed: `let the hand touch you`

Engine refused:

- `trust_delta`: delta 9 is not an integer within 2

Ledger accepted: nothing

---

## A condition the world does not have

Only sound, limping and hurt exist.

Patch proposed:

```json
{"condition": "radiant"}
```

Player typed: `keep walking`

Engine refused:

- `condition`: unknown state 'radiant'

Ledger accepted: nothing

---

## A flag that is not a flag

Flags are booleans, and their names are tokens.

Patch proposed:

```json
{"set_flags": {"ate_today": "yes", "Ate Today": true}}
```

Player typed: `eat`

Engine refused:

- `ate_today`: flag value is not a boolean
- `Ate Today`: flag name is not a snake_case token

Ledger accepted: nothing

---

